/* Auto-generated by experiments/ranking_vnext/build_live_shadow_assets.py.
 * Shadow-only scorer: this file must never change production marks or emit bets.
 */
(function(root, factory) {
  const api = factory();
  if (typeof module === 'object' && module.exports) module.exports = api;
  else root.KvVnextPartnerShadow = api;
})(typeof globalThis !== 'undefined' ? globalThis : this, function() {
  'use strict';
  const CONTRACT = Object.freeze({"schemaVersion":"kochi_partner_vnext/browser_scorer_v1","sourceSchemaVersion":"kochi_partner_vnext/shadow_v1","sourceBundleSha256":"707e63654c924f734ae6d79cc0a43872662b7483e59787c0c13a3864e70a6e39","status":"shadow_only","productionMarksAllowed":false,"valueBetAdviceAllowed":false,"selectedCandidate":"reliable_all__pointwise","factorFeatures":["jockey_changed","jockey_prior_start_count","jockey_prior_top3_rate","trainer_prior_start_count","trainer_prior_top3_rate","jockey_trainer_prior_start_count","jockey_trainer_prior_top3_rate","prior_start_count","last_finish_percentile","last_nonfinish_flag","mean_finish_percentile_last3","mean_finish_percentile_last5","history_finish_quality_proxy","finish_percentile_slope_last5","bad_run_rate_last3","cold_streak_length","nonfinish_rate_last5","top3_rate_last5","career_win_rate","career_top3_rate","rest_days","same_distance_start_count","same_distance_top3_rate","same_condition_start_count","same_condition_top3_rate","corner1_median_last5","corner4_median_last5","front4_rate_last5","agari3f_mean_last3","current_kinryo","current_body_weight","body_weight_change"],"countFeatures":["cold_streak_length","jockey_prior_start_count","jockey_trainer_prior_start_count","prior_start_count","same_condition_start_count","same_distance_start_count","trainer_prior_start_count"],"featureNames":["current_score_z","current_rank_strength","score_gap_to_anchor","rel_jockey_changed","missing__jockey_changed","rel_jockey_prior_start_count","missing__jockey_prior_start_count","rel_jockey_prior_top3_rate","missing__jockey_prior_top3_rate","rel_trainer_prior_start_count","missing__trainer_prior_start_count","rel_trainer_prior_top3_rate","missing__trainer_prior_top3_rate","rel_jockey_trainer_prior_start_count","missing__jockey_trainer_prior_start_count","rel_jockey_trainer_prior_top3_rate","missing__jockey_trainer_prior_top3_rate","rel_prior_start_count","missing__prior_start_count","rel_last_finish_percentile","missing__last_finish_percentile","rel_last_nonfinish_flag","missing__last_nonfinish_flag","rel_mean_finish_percentile_last3","missing__mean_finish_percentile_last3","rel_mean_finish_percentile_last5","missing__mean_finish_percentile_last5","rel_history_finish_quality_proxy","missing__history_finish_quality_proxy","rel_finish_percentile_slope_last5","missing__finish_percentile_slope_last5","rel_bad_run_rate_last3","missing__bad_run_rate_last3","rel_cold_streak_length","missing__cold_streak_length","rel_nonfinish_rate_last5","missing__nonfinish_rate_last5","rel_top3_rate_last5","missing__top3_rate_last5","rel_career_win_rate","missing__career_win_rate","rel_career_top3_rate","missing__career_top3_rate","rel_rest_days","missing__rest_days","rel_same_distance_start_count","missing__same_distance_start_count","rel_same_distance_top3_rate","missing__same_distance_top3_rate","rel_same_condition_start_count","missing__same_condition_start_count","rel_same_condition_top3_rate","missing__same_condition_top3_rate","rel_corner1_median_last5","missing__corner1_median_last5","rel_corner4_median_last5","missing__corner4_median_last5","rel_front4_rate_last5","missing__front4_rate_last5","rel_agari3f_mean_last3","missing__agari3f_mean_last3","rel_current_kinryo","missing__current_kinryo","rel_current_body_weight","missing__current_body_weight","rel_body_weight_change","missing__body_weight_change"],"interactions":false,"mean":[-0.20264645230000772,0.4421444223474185,-20.77
0,-0.030763822302536364,0.09866367241077224,0.19626650825497,0.0008595065671631796,0.29515055630022835,0.0008595065671631796,-0.04260103180364139,0.0008595065671631796,0.08229955712484066,0.0008595065671631796,-0.05127250295463762,0.0,-1.039992829633201,0.0,0.0024120698994284824,0.0008131028548661959],"intercept":-1.5310984979021536,"coef":[0.2619514875625144,0.24918768270365715,0.12486448984298971,-0.015123886199835514,-0.003860627213376008,0.0157026358285075,0.0,0.09169672690309785,0.00887693949544737,0.00990414234906507,0.0,-0.0031051343363977923,0.012125422893906345,-0.030877061720506464,0.0,0.02413023861894394,-0.028540749912399827,-0.06525424079273391,0.0,-0.06057348315226111,-0.003860627213376008,0.026628025448468564,-0.003860627213376008,-0.01092731243614987,-0.003860627213376008,-0.060391748301385224,-0.003860627213376008,0.06039174830138516,-0.003860627213376008,-0.01824943756518948,0.0052128319402635565,-0.056176164735681905,-0.003860627213376008,-0.09718146704901474,0.0,-0.01071584225005538,-0.003860627213376008,0.0077431246258870216,-0.003860627213376008,0.019747378001886456,-0.003860627213376008,0.040471596637714935,-0.003860627213376008,-0.062209883706680756,-0.003860627213376008,-0.063981100566335,0.0,0.09196812970724567,-0.012722339619711415,-0.030090498316873635,0.0,0.035624384782152844,0.0009411199742890322,-0.07514602049830962,0.01333158372916023,-0.05348654220115493,0.01333158372916023,0.0030049116591899495,0.01333158372916023,-0.11333301687956447,0.01333158372916023,0.00012031464234055607,0.0,0.04272930812554749,0.0,-0.07073770791888087,-0.003860627213376008]});
  const COUNTS = new Set(CONTRACT.countFeatures);
  const finite = value => {
    if (value === null || value === undefined || value === '' || typeof value === 'boolean') return null;
    const n = Number(value);
    return Number.isFinite(n) ? n : null;
  };
  const parseNumber = value => {
    if (value === null || value === undefined || typeof value === 'boolean') return null;
    if (typeof value === 'number') return Number.isFinite(value) ? value : null;
    const match = String(value).replace(/,/g, '').match(/[+-]?(?:\d+(?:\.\d*)?|\.\d+)/);
    if (!match) return null;
    const number = Number(match[0]);
    return Number.isFinite(number) ? number : null;
  };
  const round8 = value => value === null || !Number.isFinite(value)
    ? null : Math.round((value + Number.EPSILON) * 1e8) / 1e8;
  const mean = values => values.length ? round8(values.reduce((sum, value) => sum + value, 0) / values.length) : null;
  const rate = (successes, total) => total ? round8(successes / total) : null;
  const median = values => {
    if (!values.length) return null;
    const ordered = [...values].sort((a, b) => a - b);
    const middle = Math.floor(ordered.length / 2);
    return round8(ordered.length % 2 ? ordered[middle] : (ordered[middle - 1] + ordered[middle]) / 2);
  };
  const slope = values => {
    if (values.length < 2) return null;
    const xMean = (values.length - 1) / 2;
    const yMean = values.reduce((sum, value) => sum + value, 0) / values.length;
    let numerator = 0, denominator = 0;
    values.forEach((value, index) => {
      numerator += (index - xMean) * (value - yMean);
      denominator += Math.pow(index - xMean, 2);
    });
    return denominator ? round8(numerator / denominator) : null;
  };
  const ordinal = value => {
    const match = String(value || '').match(/^(\d{4})[/-](\d{1,2})[/-](\d{1,2})$/);
    return match ? Math.floor(Date.UTC(Number(match[1]), Number(match[2]) - 1, Number(match[3])) / 86400000) : null;
  };
  const sigmoid = x => x >= 0 ? 1 / (1 + Math.exp(-x)) : Math.exp(x) / (1 + Math.exp(x));

  function buildRawFeatures(context) {
    if (!context || typeof context !== 'object') return { ok:false, reason:'invalid_context' };
    const history = Array.isArray(context.history) ? context.history : [];
    const current = context.current && typeof context.current === 'object' ? context.current : {};
    const connections = context.connections && typeof context.connections === 'object' ? context.connections : {};
    const last3 = history.slice(-3);
    const last5 = history.slice(-5);
    const finishPct3 = last3.map(run => finite(run.finishPercentile)).filter(value => value !== null);
    const finishPct5 = last5.map(run => finite(run.finishPercentile)).filter(value => value !== null);
    const latest = history.length ? history[history.length - 1] : null;
    const currentDistance = parseNumber(context.currentDistance);
    const currentCondition = String(context.currentCondition || '');
    const sameDistance = history.filter(run => currentDistance !== null && parseNumber(run.distance) === currentDistance);
    const sameCondition = history.filter(run => currentCondition && String(run.trackCondition || '') === currentCondition);
    const corner1 = last5.map(run => parseNumber(run.corner1)).filter(value => value !== null && value > 0);
    const corner4 = last5.map(run => parseNumber(run.corner4)).filter(value => value !== null && value > 0);
    const currentWeight = parseNumber(current.bodyWeight);
    const previousWeight = latest ? parseNumber(latest.bodyWeight) : null;
    const currentJockey = String(current.jockey || '').trim();
    const previousJockey = latest ? String(latest.jockey || '').trim() : '';
    let coldStreak = 0;
    for (let index = history.length - 1; index >= 0; index--) {
      const finish = parseNumber(history[index].finish);
      if (finish !== null && finish <= 3) break;
      coldStreak++;
    }
    const connection = key => {
      const row = connections[key] && typeof connections[key] === 'object' ? connections[key] : {};
      const start = Math.max(0, Number.parseInt(row.start, 10) || 0);
      const top3 = Math.max(0, Number.parseInt(row.top3, 10) || 0);
      return { start, top3Rate:rate(top3, start) };
    };
    const jockey = connection('jockey');
    const trainer = connection('trainer');
    const combo = connection('combo');
    const top3Count = runs => runs.reduce((sum, run) => {
      const finish = parseNumber(run.finish);
      return sum + Number(finish !== null && finish <= 3);
    }, 0);
    const winCount = runs => runs.reduce((sum, run) => sum + Number(parseNumber(run.finish) === 1), 0);
    const currentDay = ordinal(context.currentDate);
    const previousDay = latest ? ordinal(latest.raceDate) : null;
    const raw = {
      jockey_changed: latest && currentJockey && previousJockey ? Number(currentJockey !== previousJockey) : null,
      jockey_prior_start_count: jockey.start,
      jockey_prior_top3_rate: jockey.top3Rate,
      trainer_prior_start_count: trainer.start,
      trainer_prior_top3_rate: trainer.top3Rate,
      jockey_trainer_prior_start_count: combo.start,
      jockey_trainer_prior_top3_rate: combo.top3Rate,
      prior_start_count: history.length,
      last_finish_percentile: latest ? finite(latest.finishPercentile) : null,
      last_nonfinish_flag: latest ? Number(parseNumber(latest.finish) === null) : null,
      mean_finish_percentile_last3: mean(finishPct3),
      mean_finish_percentile_last5: mean(finishPct5),
      history_finish_quality_proxy: finishPct5.length ? round8(1 - mean(finishPct5)) : null,
      finish_percentile_slope_last5: slope(finishPct5),
      bad_run_rate_last3: rate(finishPct3.filter(value => value >= 0.6).length, finishPct3.length),
      cold_streak_length: coldStreak,
      nonfinish_rate_last5: rate(last5.filter(run => parseNumber(run.finish) === null).length, last5.length),
      top3_rate_last5: rate(top3Count(last5), last5.length),
      career_win_rate: rate(winCount(history), history.length),
      career_top3_rate: rate(top3Count(history), history.length),
      rest_days: currentDay !== null && previousDay !== null ? currentDay - previousDay : null,
      same_distance_start_count: sameDistance.length,
      same_distance_top3_rate: rate(top3Count(sameDistance), sameDistance.length),
      same_condition_start_count: sameCondition.length,
      same_condition_top3_rate: rate(top3Count(sameCondition), sameCondition.length),
      corner1_median_last5: median(corner1),
      corner4_median_last5: median(corner4),
      front4_rate_last5: rate(corner4.filter(value => value <= 3).length, corner4.length),
      agari3f_mean_last3: mean(last3.map(run => parseNumber(run.agari3f)).filter(value => value !== null && value >= 25 && value <= 60)),
      current_kinryo: parseNumber(current.kinryo),
      current_body_weight: currentWeight,
      body_weight_change: currentWeight !== null && previousWeight !== null ? round8(currentWeight - previousWeight) : null,
    };
    if (CONTRACT.factorFeatures.some(name => !(name in raw))) return { ok:false, reason:'feature_contract_mismatch' };
    return { ok:true, raw };
  }

  function scoreRace(runners) {
    if (!Array.isArray(runners) || runners.length < 4) return { ok:false, reason:'fewer_than_4' };
    const rows = runners.map((runner, index) => ({
      index,
      u: Number.parseInt(runner && (runner.u ?? runner.umaBan), 10),
      name: String((runner && runner.name) || ''),
      currentScore: finite(runner && runner.currentScore),
      raw: (runner && runner.raw && typeof runner.raw === 'object') ? runner.raw : {},
    }));
    if (rows.some(row => !Number.isInteger(row.u) || row.u <= 0)) return { ok:false, reason:'invalid_uma' };
    if (new Set(rows.map(row => row.u)).size !== rows.length) return { ok:false, reason:'duplicate_uma' };
    if (rows.some(row => row.currentScore === null)) return { ok:false, reason:'missing_current_score' };
    const order = [...rows].sort((a, b) => b.currentScore - a.currentScore || a.u - b.u);
    const anchor = order[0];
    const rank = new Map(order.map((row, index) => [row.index, index + 1]));
    const scoreMean = rows.reduce((sum, row) => sum + row.currentScore, 0) / rows.length;
    const scoreSd0 = Math.sqrt(rows.reduce((sum, row) => sum + Math.pow(row.currentScore - scoreMean, 2), 0) / rows.length);
    const scoreSd = scoreSd0 < 1e-12 ? 1 : scoreSd0;
    const columns = rows.map(row => [
      (row.currentScore - scoreMean) / scoreSd,
      1 - ((rank.get(row.index) - 1) / Math.max(rows.length - 1, 1)),
      row.currentScore - anchor.currentScore,
    ]);
    for (const name of CONTRACT.factorFeatures) {
      const values = rows.map(row => {
        const value = finite(row.raw[name]);
        return value === null ? null : (COUNTS.has(name) ? Math.log1p(Math.max(value, 0)) : value);
      });
      const observed = values.filter(value => value !== null);
      const mean = observed.length ? observed.reduce((sum, value) => sum + value, 0) / observed.length : null;
      values.forEach((value, index) => {
        columns[index].push(value === null || mean === null ? null : value - mean);
        columns[index].push(value === null ? 1 : 0);
      });
    }
    if (columns.some(row => row.length !== CONTRACT.featureNames.length)) return { ok:false, reason:'design_width_mismatch' };
    const scored = rows.filter(row => row.index !== anchor.index).map(row => {
      let linear = CONTRACT.intercept;
      for (let column = 0; column < CONTRACT.coef.length; column++) {
        const value = columns[row.index][column];
        const clean = value === null || !Number.isFinite(value) ? CONTRACT.impute[column] : value;
        linear += ((clean - CONTRACT.mean[column]) / CONTRACT.sd[column]) * CONTRACT.coef[column];
      }
      return { u:row.u, name:row.name, probability:sigmoid(linear), linear };
    }).sort((a, b) => b.probability - a.probability || a.u - b.u);
    return {
      ok:true,
      status:'shadow_only',
      productionMarksAllowed:false,
      valueBetAdviceAllowed:false,
      anchor:{ u:anchor.u, name:anchor.name },
      ranked:scored,
      top3:[anchor.u, ...scored.slice(0, 2).map(row => row.u)],
    };
  }

  return Object.freeze({ contract:CONTRACT, buildRawFeatures, scoreRace });
});
